# Security Policy

## Reporting a Vulnerability

보안 취약점을 공개 Issue, Discussion, Pull Request 또는 공개 브랜치로 신고하지 마세요.
GitHub Private Vulnerability Reporting이 활성화되면 해당 채널을 사용합니다. 별도의
비공개 연락 채널이 확정되기 전에는 실제 운영 시스템을 시험하거나 상세 재현을
공개하지 마세요.

- 실제 운영 서버를 허가 없이 시험하지 않습니다.
- 제공된 시험 환경과 가짜 테스트 계정만 사용합니다.
- 실제 학생·교사 데이터에 접근하거나 내려받지 않습니다.
- 익명 신원 노출은 영향 확인에 필요한 최소 범위만 재현하고 즉시 중단합니다.
- 비밀번호, 세션 ID, 가입·재설정 코드, 암호화 키를 신고 내용에 포함하지 않습니다.

현재 비공개 신고 채널과 보안 유지관리자는 아직 확정되지 않았습니다. 이 결정이
끝나기 전에는 저장소를 운영 서비스로 공개하지 않습니다.

## System and Scope

이 정책은 저장소의 Next.js 프런트엔드, Spring Boot API, Flyway 마이그레이션,
MySQL 데이터 모델, 테스트와 CI 설정에 적용됩니다.

중요 보호 자산은 익명 작성자의 신원, 계정과 세션, 비공개 동의 관계, 개인 고충,
심의와 신원 열람 기록, 공식 답변·재정 공개 데이터의 무결성, 비밀과 백업입니다.
운영 인프라와 실제 운영 데이터는 공개 저장소 범위 밖의 별도 비공개 영역입니다.

## Threat Model and Trust Boundaries

공격자는 비인증 외부 사용자뿐 아니라 정상 학생 계정을 가진 숙련된 사용자,
탈취된 교사 계정 사용자, 권한을 오용하는 내부 운영자를 포함합니다.

- 브라우저 입력, 공개 ID, 쿠키와 헤더는 공격자 통제 값입니다.
- Next.js 프록시는 보안 경계가 아니며 백엔드가 권한을 다시 검증합니다.
- 일반 업무 데이터와 익명 Identity Vault를 분리합니다.
- 암호화 키와 데이터베이스·백업을 분리합니다.
- 공개 저장소와 비공개 운영 데이터·로그·인프라를 분리합니다.

상세 위협 모델은 `docs/security-model.md`를 따릅니다.

## Security Invariants

- 권한은 기본 거부하며 UI에서 버튼을 숨기는 것으로 권한을 대신하지 않습니다.
- 50명 미만 제안은 학생과 배정된 심의 관계자 외 교사에게 노출하지 않습니다.
- 일반 제안 API, 로그, OpenAPI 예시와 분석 이벤트에 익명 작성자 내부 ID를 넣지 않습니다.
- 신원 열람에는 독립 사건의 3인 전원 승인, 사건에 고정된 심의자 본인, 최근 재인증, 승인 시점부터 24시간이라는 기간 제한이 모두 필요합니다.
- 학생회장·부회장과 슈퍼 어드민은 실제 익명 신원을 열람할 수 없습니다.
- 공개 제한과 신원 확인은 독립된 사건과 투표를 사용합니다.
- 동의는 활성 학생 1인 1표이며 데이터베이스 고유 제약이 최종 통제입니다.
- 비밀번호와 일회 코드는 평문 또는 복호화 가능한 값으로 저장하지 않습니다.
- 감사 로그에 비밀번호, 코드 원문, 세션 ID, 키와 평문 익명 신원을 기록하지 않습니다.

## Reportable Findings and Severity Context

다음은 보고 대상입니다.

- 인증·기능·객체 권한 우회
- 익명 신원, 고충, 비공개 동의 관계 또는 세션의 노출
- 동의 위조, 중복 정식 안건 승격, 심의 우회
- 저장형 XSS, CSRF, SQL/JPQL 주입, SSRF
- 비밀·백업·로그를 통한 민감정보 노출
- 재정 공개, 공식 답변과 감사 기록의 무단 변경
- 현실적으로 도달 가능한 공급망 또는 마이그레이션 취약점

대량 신원 복호화, 원격 코드 실행, 전체 자격 증명·키·운영 DB 획득은 Critical로
평가할 수 있습니다. 타인의 고충 열람, 권한 상승, 동의 위조, 고권한 세션 탈취는
일반적으로 High입니다. 제한적 계정 열거·메타데이터 노출·서비스 거부는 도달성과
영향에 따라 Medium 또는 Low로 평가합니다.

## Out of Scope, Exclusions, and Accepted Risk

- 운영자의 명시적 허가가 없는 실제 서버 시험
- 사회공학, 피싱, 물리적 침입
- 실제 학생·교사 개인정보를 사용한 검증
- 첨부파일 기능: MVP에 존재하지 않으며 추가 시 별도 위협 모델이 필요합니다.
- 다중 서버 분산 속도 제한: 현재 단일 서버 전제이며 확장 시 재검토합니다.

이 항목은 취약점 공개나 위험한 시험을 허가하는 safe harbor가 아닙니다. 동일한
취약점이 현재 구현 경계에 현실적으로 도달한다면 보고 대상이 될 수 있습니다.
현재 승인된 보안 위험 예외는 없습니다.

## Known Limitations and Compensating Controls

- 공개 제안과 보호 심의까지 구현되었지만 비공개 고충·재정 공개와 운영 통제는 아직 개발 중이며 운영 배포 대상이 아닙니다.
- 실제 TLS, 비밀 저장소, 백업·복구와 로그 접근 정책은 아직 확정되지 않았습니다.
- 실제 CODEOWNERS와 비공개 신고 채널이 정해지기 전에는 공개 운영하지 않습니다.
- 시험은 Testcontainers와 가짜 데이터 기반 환경에서 수행합니다.
- 프런트엔드는 요청별 nonce CSP를 사용하며 일반 사용자 HTML 실행을 허용하지 않습니다.

## Maintainer Response and Coordinated Disclosure

보안 유지관리자는 비공개 신고를 접수하면 범위와 영향을 확인하고, 필요한 경우
추가 재현을 중지하도록 요청합니다. 검증 뒤 수정, 회귀 테스트, 비밀 회전 또는
데이터 보호 조치를 준비합니다. 공개가 필요한 경우 신고자와 시점·영향·완화책을
조정하고 보안 권고와 수정 릴리스를 함께 제공합니다. 실제 개인정보와 악용 가능한
세부사항은 공개 문서에서 최소화합니다.
