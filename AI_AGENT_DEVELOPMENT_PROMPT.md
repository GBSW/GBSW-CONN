# 경북소프트웨어마이스터고 학생자치 소통 시스템 — AI 개발 에이전트 마스터 프롬프트

이 문서는 AI 개발 에이전트에게 그대로 전달할 수 있는 실행 지시서이자, 새로 합류한 인간 개발자가 제품의 목적과 구조를 빠르게 이해하기 위한 개발 명세다.

프로젝트의 정식 서비스명은 아직 확정되지 않았다. 코드명이나 임의의 브랜드를 사용자 화면에 확정적으로 사용하지 말고, 기획 단계에서는 **“학교 소통 제안 시스템”**이라는 설명형 이름을 사용한다.

---

## 0. 에이전트 역할과 최상위 작업 원칙

너는 이 프로젝트의 시니어 제품 엔지니어, 보안 설계자, 백엔드·프런트엔드 개발자다. 아래 요구사항을 충실히 반영해 유지보수 가능한 실제 서비스를 설계하고 구현한다.

다음 원칙은 다른 구현 편의보다 우선한다.

1. 요구사항을 임의로 축소하거나 핵심 정책을 일반 게시판처럼 단순화하지 않는다.
2. 명시되지 않은 중요한 정책을 임의로 확정하지 않는다. 결과와 데이터 모델이 크게 달라지는 모호함이 발견되면 코딩 전에 사용자에게 질문한다.
3. 이미 확정된 정책은 다시 질문하지 않는다.
4. 처음부터 전체 기능을 한 파일이나 거대한 서비스 클래스에 구현하지 않는다.
5. 보안은 UI에서 버튼을 숨기는 방식이 아니라 서버와 데이터베이스에서 강제한다.
6. Entity를 API에 직접 노출하지 않는다. Request DTO와 Response DTO를 반드시 분리한다.
7. 기능이 동작하는 것뿐 아니라 테스트, 문서, 마이그레이션, 운영 절차까지 완료해야 구현이 끝난 것으로 본다.
8. 코드 생성 전에 현재 저장소의 파일, 기존 변경 사항, 도구와 실행 환경을 확인한다. 기존 코드가 있다면 정당한 이유 없이 구조를 교체하지 않는다.
9. 구현 중 임시 우회, 하드코딩된 사용자·역할·비밀번호·암호화 키를 남기지 않는다.
10. 배포나 외부 공개는 사용자가 별도로 승인하기 전까지 수행하지 않는다.

작업 시작 시 먼저 다음 내용을 짧게 보고한 뒤 구현한다.

- 발견한 기존 프로젝트 상태
- 구현할 단계와 순서
- 명세와 충돌하거나 추가 결정이 필요한 항목
- 사용할 정확한 안정 버전과 선택 이유

---

## 1. 제품 목적

이 서비스는 경북소프트웨어마이스터고등학교 학생과 교사가 건의, 제안, 정보 요청, 학교 개선 의견을 주고받는 학생자치 기반 소통 시스템이다.

단순 익명 게시판이나 민원 접수함이 아니다. 다음 가치를 제품 구조에 반영한다.

- 학생은 교육과 기숙사, 학교생활, 수업, 제도, 정보 공개에 대해 자유롭게 의견을 제시할 수 있다.
- 학생의 의견은 일정한 공동체 지지를 얻으면 학교가 검토해야 하는 정식 안건이 된다.
- 교사는 정식 안건의 처리 상태와 공식 답변을 투명하게 남긴다.
- 익명 표현을 허용하되, 운영자의 임의적인 신원 열람과 삭제를 기술적으로 제한한다.
- 욕설이나 비판적 표현을 사전에 자동 차단하지 않는다.
- 삭제와 신원 확인은 서로 다른 행위이며 별도의 심의가 필요하다.
- 학교 재정과 행정정보를 가능한 범위에서 학생에게 투명하게 공개한다.
- 학생의 표현 자유, 개인의 안전, 소수자의 권리, 교사의 업무 부담을 동시에 고려한다.

---

## 2. 확정 기술 스택

다음 구성을 기본값으로 사용한다.

### 프런트엔드

- Next.js
- TypeScript strict mode
- 접근 가능한 React 컴포넌트
- OpenAPI 명세에서 생성한 API 타입 또는 클라이언트

### 백엔드

- Spring Boot
- Java
- Spring Web
- Spring Security
- Spring Data JPA
- Bean Validation
- MySQL 드라이버
- Flyway 기반 데이터베이스 마이그레이션
- springdoc-openapi 호환 구성으로 Swagger/OpenAPI 제공

### 데이터베이스 및 세션

- MySQL, InnoDB
- 서버 세션 방식
- 세션 상태는 MySQL에 영속화
- Redis는 사용하지 않는다.

### 버전 선택 원칙

- 구현 시작 시점의 공식 안정 버전을 확인한다.
- Snapshot, RC, milestone 버전은 사용하지 않는다.
- Java는 지원되는 LTS 버전을 선택한다.
- Node.js도 지원되는 LTS 버전을 선택한다.
- 선택한 정확한 버전과 호환성 근거를 루트 README에 기록한다.
- 의존성 버전을 무분별하게 범위 지정하지 말고 재현 가능한 잠금 상태를 유지한다.

### 로컬 개발 환경

- 프런트엔드와 백엔드를 분리하되 하나의 저장소에서 관리한다.
- 로컬 MySQL을 간단히 실행할 수 있는 Docker Compose 구성을 제공한다.
- `.env.example` 또는 동일 목적의 예시 설정을 제공하되 실제 비밀정보를 포함하지 않는다.

권장 최상위 구조:

```text
project-root/
├── .github/
│   ├── CODEOWNERS
│   ├── ISSUE_TEMPLATE/
│   └── pull_request_template.md
├── frontend/
├── backend/
├── docs/
├── docker-compose.yml
├── README.md
├── SECURITY.md
├── CONTRIBUTING.md
├── CODE_OF_CONDUCT.md
├── GOVERNANCE.md
├── LICENSE
├── NOTICE
└── .env.example
```

---

## 2.1 오픈 소스 공개 원칙

이 프로젝트는 장기적으로 오픈 소스 프로젝트로 운영한다. 공개의 목적은 학생자치 시스템의 투명성을 높이고, 졸업 이후에도 후배와 외부 기여자가 유지보수하며, 다른 학교가 재사용할 수 있게 하는 것이다.

오픈 소스화는 운영 데이터와 운영 인프라까지 공개한다는 뜻이 아니다. **공개 애플리케이션 저장소와 비공개 운영 영역을 분리한다.**

### 공개 저장소에 포함할 수 있는 것

- 프런트엔드와 백엔드 소스코드
- DB 스키마와 Flyway 마이그레이션
- 가짜 사용자만 포함한 시드 데이터
- OpenAPI/Swagger 명세
- 로컬 Docker Compose
- 테스트 코드
- 아키텍처, 권한표, 상태 전이, 보안 위협 모델
- `.env.example`
- 일반적인 배포·운영 가이드

### 공개 저장소에 포함하면 안 되는 것

- 실제 학생·교사 계정과 명단
- 학번, 연락처, 비밀번호 해시를 포함한 운영 DB
- 익명 작성자 연결정보
- 실제 동의자 기록
- 운영 DB 백업
- 암호화 키, HMAC 키, 세션 비밀값
- 학교알리미 및 다른 외부 서비스 API 키
- 실제 `.env`
- 운영 로그와 감사 로그
- 서버 IP, 내부망 구성, 관리자 접속정보
- 미공개 취약점의 상세 재현 절차

운영 배포 설정이 필요하면 별도의 접근 제한 저장소로 분리한다. 비공개 저장소에도 비밀값 원문을 커밋하지 말고 GitHub Secrets, 운영체제 보안 저장소 또는 승인된 비밀관리 도구를 사용한다.

### 라이선스

- 애플리케이션 코드의 기본 라이선스는 `Apache-2.0`으로 한다.
- `LICENSE`에 공식 Apache License 2.0 원문을 정확히 포함한다.
- 필요한 저작권과 고지는 `NOTICE`에 관리한다.
- 학교 이름, 교표, 로고, 기타 상표성 자산은 코드 라이선스에 포함된 것으로 간주하지 않는다.
- 학교명·로고를 공식적으로 사용하기 전에 권리와 승인 여부를 확인한다.
- 저작권 주체가 확정되지 않았다면 임의의 학교·재단·개인 명의로 저작권 문구를 작성하지 말고 사용자에게 확인한다.

### 오픈 소스 필수 문서

- `LICENSE`
- `NOTICE`
- `README.md`
- `CONTRIBUTING.md`
- `CODE_OF_CONDUCT.md`
- `SECURITY.md`
- `GOVERNANCE.md`
- `.github/CODEOWNERS`
- Issue 템플릿
- Pull Request 템플릿

`SECURITY.md`에는 다음을 명확히 적는다.

- 보안 취약점을 공개 Issue로 신고하지 않는다.
- GitHub Private Vulnerability Reporting 또는 지정된 비공개 채널을 사용한다.
- 실제 운영 서버를 허가 없이 시험하지 않는다.
- 제공된 시험 환경과 테스트 계정만 사용한다.
- 익명 신원 노출 취약점은 재현을 최소화하고 즉시 비공개 신고한다.
- 유지관리자의 접수, 확인, 수정, 조정된 공개 절차를 설명한다.

### GitHub 저장소 운영

- 가능하면 개인 계정이 아닌 GitHub Organization이 저장소를 소유한다.
- Organization Owner는 최소 두 명을 두어 한 명의 졸업·이탈·계정 분실이 프로젝트 소유권 상실로 이어지지 않게 한다.
- Secret scanning과 push protection을 활성화한다.
- Private Vulnerability Reporting과 Repository Security Advisory를 활성화한다.
- 의존성 취약점 알림과 자동화된 보안 업데이트를 검토한다.
- `CODEOWNERS`로 인증, 권한, 암호화, 심의, 마이그레이션, GitHub 설정 파일의 검토 책임자를 지정한다.
- `main`과 이후 도입되는 `develop`을 보호하고 직접 push, force push, 임의 삭제를 차단한다.
- 필수 테스트와 승인 없이 병합하지 않는다.

공개 전에는 전체 Git 기록에서 비밀정보와 개인정보를 검사한다. 비밀값이 한 번이라도 커밋되었다면 파일만 삭제하는 것으로 끝내지 말고 해당 비밀을 즉시 폐기·교체한 뒤 공개 여부를 재검토한다.

---

## 2.2 단계별 Git 브랜치 전략

브랜치 전략은 프로젝트 규모와 릴리스 단계에 따라 전환한다. MVP 초기부터 고전적인 GitFlow 전체를 적용하지 않는다.

### 1단계 — MVP 개발: 단순 Feature Branch 방식

MVP가 완성되고 첫 안정 릴리스가 나오기 전까지는 다음 구조를 사용한다.

- 장기 브랜치는 `main` 하나만 유지한다.
- `main`은 항상 빌드와 테스트가 통과하는 상태를 유지한다.
- 모든 작업은 짧게 유지되는 작업 브랜치에서 진행한다.
- 모든 변경은 Pull Request를 통해 `main`에 병합한다.
- 작업 브랜치는 가능한 한 며칠 이내에 병합하거나 폐기하며 장기간 분기시키지 않는다.
- `main` 직접 push와 force push를 금지한다.
- Pull Request에는 관련 Issue, 변경 이유, 시험 결과, 보안 영향, 문서 변경 여부를 기록한다.
- 기능 브랜치 병합은 기본적으로 squash merge를 사용해 MVP 이력을 읽기 쉽게 유지한다.

브랜치 이름:

```text
feat/<issue-number>-<short-description>
fix/<issue-number>-<short-description>
refactor/<issue-number>-<short-description>
test/<issue-number>-<short-description>
docs/<issue-number>-<short-description>
chore/<issue-number>-<short-description>
```

MVP 단계에서는 `develop`, `release/*`, `hotfix/*`를 만들지 않는다. 빈 브랜치 체계를 미리 유지하는 데 시간을 쓰지 않는다.

### 2단계 — 안정적인 오픈 소스 운영: 경량 GitFlow

다음 조건이 충족되면 경량 GitFlow로 전환한다.

- 실제 학교 운영에 사용되는 안정 릴리스가 존재한다.
- 최소 `v1.0.0`에 준하는 공개 릴리스 기준이 정해졌다.
- 외부 기여자 또는 여러 유지관리자가 동시에 작업하기 시작했다.
- 다음 버전 개발과 현재 운영 버전의 긴급 수정이 병행될 필요가 있다.

단순히 저장소를 공개했다는 이유만으로 전환하지 않는다. 전환 시점과 결정 이유는 `GOVERNANCE.md`와 Architecture Decision Record에 남긴다.

경량 GitFlow의 브랜치 책임:

- `main`: 운영 가능한 공식 릴리스 이력만 보관한다. 모든 릴리스에 `vMAJOR.MINOR.PATCH` 태그를 붙인다.
- `develop`: 다음 릴리스를 위한 통합 브랜치다.
- `feature/*`: `develop`에서 분기해 `develop`으로 Pull Request를 보낸다.
- `release/<version>`: `develop`에서 분기하며 버전 고정, 문서, 마이그레이션 검증, 결함 수정만 허용한다. 새 기능을 추가하지 않는다.
- `hotfix/<version>`: 운영 중인 `main`에서 분기해 긴급 수정 후 `main`과 `develop` 양쪽에 반영한다.

병합 규칙:

1. 기능 개발은 `feature/* → develop`으로 병합한다.
2. 릴리스 준비는 `develop → release/<version>`으로 분기한다.
3. 릴리스 검증 후 `release/<version> → main`을 병합하고 태그를 생성한다.
4. 같은 릴리스 변경을 `develop`에도 반드시 되돌려 반영한다.
5. 긴급 수정은 `main → hotfix/<version>`으로 분기한다.
6. 검증된 hotfix는 `main`과 `develop`에 모두 병합한다.
7. 장기 feature 브랜치를 피하고 `develop`과 자주 동기화한다.

보호 규칙:

- `main`과 `develop` 모두 직접 push를 금지한다.
- 필수 CI, 대화 해결, 승인된 리뷰를 요구한다.
- 인증, 권한, 익명 신원, 암호화, 감사 로그, 마이그레이션 변경은 CODEOWNERS 승인을 요구한다.
- PR 작성자가 자신의 변경을 단독 승인해 병합하지 못하게 한다.
- `main`에는 릴리스 및 hotfix 외 일반 기능 PR을 직접 병합하지 않는다.

### 보안 취약점 수정 브랜치

공개 저장소에서 심각한 미공개 취약점을 `security/*` 공개 브랜치로 수정하지 않는다. GitHub Repository Security Advisory의 임시 비공개 포크에서 협업하고, 패치와 권고문을 준비한 뒤 조정된 방식으로 공개한다.

### 릴리스 관리

- Semantic Versioning 형식의 태그를 사용한다.
- 각 릴리스에 변경 내역, 마이그레이션 주의사항, 보안 수정, 호환성 정보를 제공한다.
- Git 태그만 만들고 릴리스 문서를 생략하지 않는다.
- 운영 DB 마이그레이션과 롤백 또는 복구 절차를 릴리스 전에 검증한다.

---

## 3. 백엔드 구조와 코딩 규칙

최상위에서 `entity`, `controller`, `service`를 전부 한곳에 모으는 전역 계층형 구조를 만들지 않는다. **도메인/기능별로 먼저 나누고, 각 도메인 내부에서 계층을 분리한다.**

권장 패키지 구조:

```text
backend/src/main/java/<base-package>/
├── common/
│   ├── config/
│   ├── security/
│   ├── exception/
│   ├── response/
│   ├── validation/
│   └── util/
├── auth/
├── user/
├── office/
├── proposal/
├── support/
├── moderation/
├── identity/
├── privatecase/
├── disclosure/
├── finance/
└── audit/
```

각 도메인은 필요에 따라 다음 구조를 가진다.

```text
proposal/
├── controller/
├── service/
├── repository/
├── domain/
│   └── entity/
├── dto/
│   ├── request/
│   └── response/
├── mapper/
└── exception/
```

### 계층별 책임

- **Controller**: HTTP 요청 파싱, DTO 검증, 인증 사용자 전달, 응답 변환만 담당한다.
- **Service**: 업무 규칙, 권한 결정, 상태 전이, 트랜잭션 경계를 담당한다.
- **Repository**: 데이터 조회와 저장만 담당한다. 정책 판단을 넣지 않는다.
- **Entity**: 영속 상태와 최소한의 도메인 불변조건을 표현한다.
- **Request DTO**: 클라이언트 입력만 표현하며 허용 필드를 명시한다.
- **Response DTO**: 화면에 필요한 값만 반환한다. 내부 UUID, 암호화 데이터, 감사용 필드를 포함하지 않는다.
- **Mapper**: Entity와 DTO 변환을 담당한다. 무분별한 리플렉션 기반 자동 매핑은 피한다.
- **Domain Exception**: 예상 가능한 업무 오류를 구체적인 오류 코드로 표현한다.
- **Global Exception Handler**: 예외를 일관된 API 오류 형식으로 변환하고 스택 트레이스를 외부에 노출하지 않는다.

### 필수 코드 규칙

- 모든 쓰기 업무는 적절한 `@Transactional` 경계 안에서 처리한다.
- 읽기 전용 서비스는 가능한 경우 읽기 전용 트랜잭션을 사용한다.
- 요청 DTO와 응답 DTO를 같은 클래스로 재사용하지 않는다.
- JPA Entity를 Controller가 직접 반환하지 않는다.
- 권한 검사는 Controller URL 수준과 Service 메서드 수준에서 방어적으로 수행한다.
- 서비스에서 클라이언트가 보낸 역할, 작성자 ID, 투표 수를 신뢰하지 않는다.
- 시간은 서버 기준으로 생성하고 DB 저장은 일관된 시간대 기준을 사용한다.
- 사용자에게 노출하는 날짜는 Asia/Seoul 기준으로 표시한다.
- 식별자는 외부 노출용과 내부용을 분리할 수 있도록 설계한다.

---

## 4. 사용자, 역할, 보직

역할을 단순한 상하 계층으로 구현하지 말고 권한 묶음으로 구현한다.

### 기본 계정 역할

- `STUDENT`
- `TEACHER`
- `SUPER_ADMIN`

### 임기 기반 보직

- `STUDENT_AFFAIRS_TEACHER` — 학생부장 교사
- `STUDENT_COUNCIL_PRESIDENT` — 전교 학생회장
- `STUDENT_COUNCIL_VICE_PRESIDENT` — 전교 학생부회장

학생회장과 부회장은 별도의 계정을 만들지 않는다. 일반 학생 계정에 임기 기반 보직을 추가한다. 따라서 학생회 임원도 제안 작성과 동의 시 일반 학생과 정확히 같은 권한과 화면을 사용한다.

### 슈퍼 어드민 원칙

슈퍼 어드민은 다음을 할 수 있다.

- 사용자 계정 생성, 활성화, 정지
- 학생·교사 역할 지정
- 학생부장, 학생회장, 학생부회장 보직 임명
- 다음 학기·학년도 보직 예약 및 교체
- 시스템 운영 설정 관리

슈퍼 어드민이라는 이유만으로 익명 작성자의 신원을 임의로 열람할 수 있어서는 안 된다. 슈퍼 어드민의 역할 지정, 계정 정지, 보직 변경은 모두 감사 로그에 남긴다.

### 보직 임기

보직은 단일 현재 값으로 덮어쓰지 않는다. 다음 정보를 포함하는 이력으로 관리한다.

- 보직 종류
- 담당 사용자
- 시작 시각
- 종료 시각
- 임명한 슈퍼 어드민
- 임명 시각
- 변경 사유

심의가 시작된 사건의 심의위원은 중간에 보직이 변경되어도 사건 생성 시점의 담당자로 고정한다.

---

## 5. 자체 로그인과 계정 생명주기

외부 학교 계정이나 Google 로그인을 MVP의 필수 인증 방식으로 사용하지 않는다. 자체 로그인 시스템을 구축한다. 향후 Google/OIDC 연결이 가능하도록 인증 도메인은 나머지 업무 로직과 분리한다.

### 계정 생성 및 최초 활성화

1. 슈퍼 어드민이 학생 또는 교사 계정을 생성한다.
2. 서버가 사용자별로 서로 다른 일회용 가입 코드를 생성한다.
3. 가입 코드는 모호한 문자를 제외한 10~12자리 이상의 안전한 무작위 값으로 생성한다.
4. 가입 코드는 원문 저장하지 않고 비밀번호와 마찬가지로 안전하게 해시한다.
5. 가입 코드는 한 번 사용하면 즉시 폐기한다.
6. 가입 코드는 설정된 기간이 지나면 만료된다.
7. 사용자는 로그인 ID와 가입 코드를 입력하고 본인 비밀번호를 설정한다.
8. 최초 비밀번호 설정이 완료되기 전에는 일반 서비스 기능을 사용할 수 없다.

모든 사용자에게 같은 초기 비밀번호를 발급하는 기능은 절대 만들지 않는다.

### 비밀번호

- Argon2id를 사용해 저장한다.
- 평문, 복호화 가능한 암호문, 빠른 단순 해시로 저장하지 않는다.
- 충분한 길이의 비밀번호 또는 패스프레이즈를 허용한다.
- 관리자도 사용자의 비밀번호를 확인할 수 없다.
- 비밀번호 분실 시 관리자가 새 비밀번호를 지정해 알려주는 대신 일회용 재설정 코드를 발급한다.
- 비밀번호 변경, 재설정, 역할 상승 후 기존 세션을 적절히 폐기한다.

### 로그인 방어

- 로그인 실패 횟수는 계정과 IP 양쪽 관점에서 제한한다.
- 로그인, 가입 코드, 비밀번호 재설정에 대한 중요 차단 상태는 MySQL에 저장해 서버 재시작 후에도 유지한다.
- 계정 존재 여부를 추측할 수 없도록 로그인과 재설정 오류 메시지를 일반화한다.
- 민감한 작업 전에는 최근 재인증을 요구한다.
- 로그에는 비밀번호, 가입 코드, 세션 ID, 암호화 키를 절대 기록하지 않는다.

### 세션

- 서버가 생성한 예측 불가능한 불투명 세션 ID를 사용한다.
- 세션 쿠키에 `HttpOnly`, `Secure`, 적절한 `SameSite`를 설정한다.
- 프로덕션에서는 HTTPS만 허용한다.
- 로그인 성공, 비밀번호 변경, 권한 변경 시 세션 고정 공격을 막기 위해 세션 ID를 재발급한다.
- 로그아웃과 관리자 계정 정지 시 세션을 서버에서 무효화한다.
- 세션과 사용자 권한의 불일치가 생기지 않도록 역할 변경 시 활성 세션을 재평가하거나 폐기한다.

---

## 6. 서비스 기능 분류

학생이 제출하는 내용은 성격에 따라 최소 두 경로로 나눈다.

### A. 공개 학교 개선 제안

- 학생에게 공개된다.
- 학생들의 동의를 받을 수 있다.
- 50명 이상의 동의를 받으면 정식 안건으로 승격된다.
- 50명 미만일 때는 일반 교사에게 본문이 보이지 않는다.
- 정식 안건이 되면 교사가 검토하고 공식 상태와 답변을 등록한다.

예시:

- 수업 강화 또는 개편 제안
- 기숙사 생활 개선
- 학생자치 제도 개선
- 학교시설 또는 운영방식 개선
- 학생 대상 정보 공개 요청

### B. 비공개 개인 고충·안전 제보

- 50명 동의를 요구하지 않는다.
- 일반 학생에게 공개되지 않는다.
- 학교폭력, 자해 위험, 개인정보 노출, 성희롱, 차별, 개인 권리 침해 등 다수결로 처리하면 안 되는 사안을 위한 경로다.
- 최소 권한 원칙에 따라 지정된 담당자만 열람한다.
- 공개 제안과 데이터·권한·화면을 분리한다.

MVP에서 두 경로를 혼동하게 만드는 단일 작성 폼을 만들지 않는다. 작성 시작 시 사용자가 두 경로의 차이를 짧고 명확하게 이해할 수 있어야 한다.

---

## 7. 공개 제안 작성과 익명성

모든 제안 작성자는 로그인된 사용자여야 한다.

작성 시 다음 중 하나를 선택할 수 있다.

- **익명 공개**: 다른 학생과 일반 교사에게 작성자 이름을 표시하지 않는다.
- **실명 공개**: 작성자가 선택한 경우에만 이름을 표시한다.

“익명”은 외부와 일반 운영 화면에 대한 익명이다. 시스템은 계정 남용 대응을 위해 작성자 연결 정보를 보호된 영역에 보유한다.

### 익명 작성자 데이터

- Proposal의 일반 조회 모델에 평문 사용자 ID를 포함하지 않는다.
- 작성자 연결 정보는 별도의 Identity/Vault 성격의 데이터 구조로 분리한다.
- 필요한 경우 애플리케이션 계층에서 인증된 암호화 방식을 사용한다.
- 암호화 키는 소스코드, Git, DB에 저장하지 않는다.
- API 응답, 로그, 예외, 검색 색인, 분석 이벤트에 익명 작성자의 내부 ID를 포함하지 않는다.
- 작성자 본인의 수정·조회 권한은 서버에서 안전하게 확인한다.

### 콘텐츠 정책

- 욕설, 비판, 부정적인 표현을 작성 단계에서 자동 차단하지 않는다.
- AI 기반 사전 검열이나 금칙어 자동 삭제를 넣지 않는다.
- 원문 HTML 실행을 허용하지 않는다.
- 사용자가 입력한 문자열은 저장형 XSS가 발생하지 않도록 출력 시 안전하게 처리한다.
- 신고 기능은 제공하되 신고만으로 즉시 영구 삭제하지 않는다.

### MVP 제외

첨부파일 업로드는 MVP에서 제외한다. 향후 추가 시 파일 형식 화이트리스트, 크기 제한, 무작위 저장명, 웹루트 외부 저장, 악성 파일 검사, 다운로드 권한 검사가 선행되어야 한다.

---

## 8. 50명 동의와 정식 안건 승격

공개 제안은 다음 흐름을 따른다.

```text
제안 등록
→ 학생에게 공개
→ 동의 모집
→ 50명 도달
→ 정식 안건으로 원자적 승격
→ 교사 검토
→ 공식 답변 및 결정
→ 실행 상태 추적
```

### 동의 규칙

- 현재 활성화된 학생 계정만 동의할 수 있다.
- 교사와 슈퍼 어드민 역할만 가진 계정은 학생 동의를 행사할 수 없다.
- 학생회장과 부회장의 동의도 일반 학생과 같은 한 표다.
- 작성자는 제안 생성 시 자동으로 동의한 것으로 처리하며 1표에 포함한다.
- 한 학생은 같은 제안에 한 번만 동의할 수 있다.
- 50명 미만 단계에서는 사용자가 본인의 동의를 철회할 수 있다.
- 50명에 도달해 정식 안건이 된 뒤에는 동의 철회로 일반 제안으로 되돌아가지 않는다.
- 동의자 명단은 다른 사용자에게 공개하지 않고 총 동의 수만 표시한다.
- 일반 API 응답에 동의자의 내부 사용자 ID를 포함하지 않는다.

### 중복 동의 방지

중복 동의 방지의 최종 통제는 애플리케이션의 사전 조회가 아니라 MySQL 고유 제약조건이어야 한다.

```text
UNIQUE (proposal_id, voter_user_id)
```

- `voter_user_id`는 학번이나 로그인 ID가 아닌 서버 내부 무작위 UUID다.
- 내부 UUID는 공개 URL과 응답에 노출하지 않는다.
- 같은 사용자가 요청을 병렬로 여러 번 보내도 한 건만 성공해야 한다.
- 중복 요청은 멱등적으로 처리해 사용자에게 일관된 결과를 준다.
- 투표 수는 클라이언트가 보낸 값이 아니라 DB의 유효 동의 건수에서 계산한다.
- 동의 기록은 DB 및 백업 암호화와 엄격한 접근 권한으로 보호한다.

한 사용자가 다른 학생 계정 여러 개를 탈취한 경우는 고유 제약만으로 막을 수 없다. 계정 탈취 방지, 로그인 이상 징후, 계정 잠금과 감사 로그로 대응한다.

### 50명 승격의 동시성

- 50번째 근처에서 여러 동의 요청이 동시에 들어와도 정식 안건은 정확히 한 번만 생성한다.
- 승격 조건 확인, 상태 변경, 상태 이력 기록, 교사 알림 생성을 하나의 트랜잭션으로 처리한다.
- DB 제약과 적절한 잠금 또는 원자적 갱신을 사용한다.
- 승격 시각과 당시 유효 동의 수를 스냅샷으로 보존한다.

### 공개 범위

- 50명 미만 제안: 학생에게 공개, 일반 교사에게 비공개
- 50명 이상 정식 안건: 학생과 교사에게 공개
- 신고된 50명 미만 제안: 지정된 심의 관계자에게만 예외적으로 검토 권한 부여

---

## 9. 제안 처리 상태

제안의 “업무 처리 상태”와 “공개 상태”를 하나의 enum에 섞지 않는다.

### 업무 처리 상태 예시

- `GATHERING_SUPPORT` — 동의 모집 중
- `FORMAL_AGENDA` — 50명 도달, 정식 안건
- `UNDER_REVIEW` — 교사 검토 중
- `ACCEPTED` — 채택
- `ON_HOLD` — 보류
- `REJECTED` — 반려
- `IN_PROGRESS` — 실행 중
- `COMPLETED` — 실행 완료

### 공개 상태 예시

- `VISIBLE`
- `RESTRICTED`
- `HIDDEN_BY_DECISION`

업무 상태 전이는 서버에서 허용 목록으로 강제한다. 임의의 상태 문자열을 받아 그대로 저장하지 않는다. 모든 상태 변경은 변경자, 변경 전후 상태, 시각, 사유를 이력으로 남긴다.

교사의 공식 답변에는 최소 다음 정보가 포함되어야 한다.

- 공식 답변 내용
- 담당 또는 응답 교사
- 결정 상태
- 결정 사유
- 답변 시각
- 후속 계획 또는 실행 일정(해당하는 경우)

정확한 답변 기한은 아직 확정되지 않았다. 코드에 숫자를 하드코딩하지 말고 운영 설정으로 분리하거나 명세 확정 전 사용자에게 질문한다.

---

## 10. 신고, 공개 제한, 삭제, 신원 확인

다음 행위를 명확히 분리한다.

1. 신고 접수
2. 공개 제한 또는 숨김 심의
3. 신원 확인 심의
4. 실제 신원 열람

신고되었다는 이유만으로 자동 삭제하거나 자동으로 작성자 신원을 공개하지 않는다.

### 고정 심의위원

신원 확인과 민감한 공개 제한 심의는 사건 생성 시점의 다음 보직자 세 명에게 배정한다.

- 학생부장 교사 1명
- 전교 학생회장 1명
- 전교 학생부회장 1명

세 명 모두 승인해야 심의가 통과한다.

### 공개 제한·삭제

- “삭제”는 기본적으로 물리적인 DB 영구 삭제가 아니라 일반 사용자에게 보이지 않도록 공개 상태를 변경하는 소프트 삭제다.
- 감사와 이의 제기를 위해 원문과 결정 기록을 보존한다.
- 공개 제한과 신원 확인은 서로 별도의 안건과 투표를 가진다.
- 각 심의자는 승인 또는 거부와 사유를 남긴다.
- 이미 결정된 심의 기록을 업데이트해 덮어쓰지 않는다.

### 신원 확인

1. 권한 있는 사용자가 사유를 포함해 신원 확인 사건을 생성한다.
2. 사건 생성 시 학생부장, 학생회장, 학생부회장을 스냅샷으로 배정한다.
3. 세 명이 각각 승인 또는 거부한다.
4. 세 명 모두 승인한 경우에만 신원 열람이 가능해진다.
5. 학생회장과 부회장은 승인 여부만 판단하며 실제 이름·학번을 볼 수 없다.
6. 학생부장 교사만 최근 재인증 후 실제 신원을 열람할 수 있다.
7. 열람 시각, 사건, 열람자, 사유를 감사 로그에 남긴다.
8. 일반 조회 API나 심의 응답 DTO에 신원 정보를 포함하지 않는다.

심의 절차를 프런트엔드 화면만으로 강제하지 않는다. 백엔드가 현재 보직, 배정된 심의자, 이미 행사한 표, 세 명의 최종 결과를 모두 검증해야 한다.

### 학기·학년도 교체

- 슈퍼 어드민은 다음 임기의 학생부장, 학생회장, 부회장을 임명할 수 있다.
- 기존 임명 기록은 삭제하지 않는다.
- 새 임기는 시작 시각부터 새로운 사건에 적용한다.
- 기존 사건에는 사건 생성 당시 심의위원을 유지한다.

---

## 11. 학교 재정·행정 공개

이 기능을 단순 “공문 게시판”으로 부르지 말고 **학교 재정·행정 공개** 영역으로 설계한다.

### 공개 대상

- 학교회계 예산
- 학교회계 결산
- 학교발전기금 등 공식 공시정보
- 학교가 학생에게 공개하기로 한 행사·사업·강사 초청·계약·구매의 집행 내역
- 원본 문서 또는 공식 출처 링크

### 데이터 출처 우선순위

1. 공식 Open API
2. 학교가 제공하는 검증된 내부 연동 데이터
3. 권한 있는 교사의 직접 등록

학교알리미 Open API를 통해 얻을 수 있는 예·결산 등 공식 공시정보는 어댑터로 분리한다. API 키와 정확한 제공 항목이 준비되지 않았으면 데이터를 만들어내거나 비공식 페이지를 무단 스크래핑하지 않는다. 외부 연동을 비활성화한 상태에서도 담당자가 검증된 자료를 직접 등록할 수 있어야 한다.

### 재정 공개 항목

- 제목 또는 사업명
- 목적
- 예산인지 실제 집행액인지 구분
- 금액
- 집행일 또는 기준일
- 분류
- 공개 가능한 범위의 계약 상대방 또는 초청 대상
- 설명
- 공식 출처 URL
- 원본 문서 메타정보
- 등록자와 검증자
- 최종 수정 시각

예산과 실제 지출을 같은 의미로 표시하지 않는다. 개인정보와 비공개 대상 정보는 권한 있는 담당자가 공개 전에 검토할 수 있어야 한다.

---

## 12. 우선 검토할 도메인 모델

아래 목록을 그대로 무비판적으로 생성하지 말고, 중복 책임과 관계를 검토한 ERD를 먼저 작성한다. 다만 핵심 개념을 누락해서는 안 된다.

- `User`
- `Credential`
- `ActivationCode`
- `PasswordResetToken`
- `RoleAssignment`
- `OfficeAssignment`
- `Proposal`
- `ProposalIdentity`
- `ProposalSupport`
- `ProposalStatusHistory`
- `OfficialResponse`
- `PrivateCase`
- `ContentReport`
- `ModerationCase`
- `ModerationReviewerSnapshot`
- `ModerationVote`
- `IdentityRevealRecord`
- `FinanceDisclosure`
- `ExternalDisclosureSyncRecord`
- `AuditLog`

### 데이터 모델 원칙

- 사용자 내부 식별자는 예측하기 어려운 UUID를 사용한다.
- 로그인 ID, 학번, 내부 UUID를 같은 값으로 사용하지 않는다.
- 업무 데이터의 생성자·변경자는 필요한 경우 감사 가능하게 기록한다.
- 개인정보와 익명 연결 정보는 일반 업무 테이블과 분리한다.
- 소프트 삭제와 업무 상태를 구분한다.
- 상태 이력과 심의 투표는 수정 가능한 현재 값 하나로만 보관하지 않는다.
- DB 제약조건으로 가능한 불변조건은 애플리케이션 검사에만 의존하지 않는다.
- Flyway 마이그레이션은 순서대로 재현 가능해야 하며 이미 적용된 마이그레이션을 수정하지 않는다.
- 운영 데이터 초기화를 위한 destructive 명령을 기본 스크립트에 넣지 않는다.

구현 전 `docs/erd.md` 또는 동등한 문서에 ERD와 주요 제약조건을 작성한다.

---

## 13. REST API 설계

- 기본 경로에 버전을 포함한다. 예: `/api/v1`.
- 리소스 중심 URL을 사용한다.
- Controller마다 임의의 응답 형식을 만들지 않는다.
- 페이지네이션, 정렬, 검색 규칙을 통일한다.
- 오류 코드는 사람이 읽는 메시지와 분리한다.
- 권한이 없을 때 존재 여부를 불필요하게 노출하지 않는다.
- 내부 Entity 필드 전체를 직렬화하지 않는다.
- 상태 변경 API는 허용된 명령을 명시적으로 표현한다.
- 삭제와 신원 확인을 같은 API로 처리하지 않는다.
- 동의 생성은 중복 요청에 대해 멱등적으로 동작한다.

### 공통 오류 응답 예시

```text
ErrorResponse
- code: 안정적인 기계 판독용 오류 코드
- message: 사용자 또는 개발자용 안전한 메시지
- timestamp
- traceId
- fieldErrors: 입력 검증 오류가 있을 때만 제공
```

응답에 Java 클래스명, SQL, 파일 경로, 스택 트레이스, 내부 식별자를 포함하지 않는다.

---

## 14. Swagger / OpenAPI 필수 요구사항

Swagger는 부가 기능이 아니라 개발자 온보딩과 프런트엔드 계약의 핵심 산출물이다.

### 기본 설정

- springdoc-openapi를 사용해 OpenAPI 3 명세를 생성한다.
- 개발 환경에서 `/v3/api-docs`와 Swagger UI를 제공한다.
- 정확한 Swagger UI 경로를 README에 기록한다.
- 프로덕션에서는 Swagger를 기본 공개하지 않는다. 비활성화하거나 승인된 관리자·내부 네트워크로 제한한다.
- API 제목, 설명, 버전, 서버 환경을 설정한다.
- 세션 쿠키 기반 인증 방식을 OpenAPI Security Scheme으로 문서화한다.
- 로그인 후 Swagger UI 요청에 세션 쿠키와 필요한 CSRF 정보가 올바르게 포함되도록 개발 환경을 구성한다.

### 문서화 품질

모든 외부 API에 다음을 작성한다.

- `@Operation` 수준의 목적 설명
- 필요한 역할과 권한
- 성공 응답
- 입력 검증 실패
- 인증 실패
- 권한 부족
- 리소스 없음
- 상태 충돌
- 속도 제한
- Request/Response schema 설명
- 실제 형태와 일치하는 예시

특히 다음 흐름은 Swagger만 보고도 시험할 수 있어야 한다.

- 계정 활성화
- 로그인과 로그아웃
- 비밀번호 재설정
- 익명·실명 제안 생성
- 제안 목록과 상세 조회
- 동의·철회
- 50명 승격 이후 교사 조회
- 공식 답변과 상태 변경
- 신고 생성
- 공개 제한 심의
- 신원 확인 심의
- 학생부장 신원 열람
- 슈퍼 어드민의 보직 임명
- 재정·행정 공개 조회와 등록

### 프런트엔드 계약

- 생성된 OpenAPI JSON을 기반으로 TypeScript 타입 또는 API 클라이언트를 생성한다.
- 프런트엔드가 수기로 백엔드 DTO를 중복 정의하지 않도록 한다.
- API 명세가 변경되면 타입 생성과 컴파일 검사가 실패하도록 스크립트를 제공한다.
- CI 또는 검증 스크립트에서 OpenAPI 생성과 프런트 타입 동기화를 확인한다.

---

## 15. 프런트엔드 정보 구조와 UX

디자인 원칙은 Dieter Rams의 **Less, but better**와 절제된 스칸디나비안 접근을 따른다.

### 시각 원칙

- 장식보다 정보와 행동을 우선한다.
- 과도한 카드, 그림자, 둥근 모서리, 그라데이션, 글로우, 유리 효과를 사용하지 않는다.
- 의미 없이 모든 영역을 카드로 감싸는 AI식 대시보드를 만들지 않는다.
- 불필요한 아이콘과 일러스트를 넣지 않는다.
- 제한된 중립 색상과 명확한 상태 색상을 사용한다.
- 타이포그래피, 여백, 정렬, 대비로 위계를 만든다.
- 모바일과 데스크톱에서 핵심 흐름이 동일하게 짧아야 한다.
- 접근성 있는 포커스 상태, 키보드 탐색, 오류 메시지, 라벨을 제공한다.
- 상태를 색상만으로 표현하지 않는다.
- 실제 제품 문구를 사용하고 lorem ipsum이나 과장된 마케팅 문구를 사용하지 않는다.

### 핵심 화면

- 로그인
- 최초 계정 활성화 및 비밀번호 설정
- 공개 제안 피드
- 제안 작성 경로 선택
- 공개 제안 작성
- 비공개 개인 고충·안전 제보
- 제안 상세, 동의, 진행 상태, 공식 답변
- 정식 안건 목록
- 본인이 작성하거나 동의한 내용
- 교사 정식 안건 검토함
- 심의위원 사건함
- 학생부장 신원 열람 화면
- 슈퍼 어드민 계정·역할·보직 관리
- 학교 재정·행정 공개 목록과 상세

### 첫 화면 우선순위

로그인 후 첫 화면은 일반적인 관리자 대시보드가 아니라 학생이 가장 자주 하는 일에 집중한다.

- 새로운 제안을 탐색한다.
- 제안에 동의한다.
- 제안을 작성한다.
- 정식 안건과 학교 답변을 확인한다.

교사 계정은 50명 이상 정식 안건을 중심으로 진입한다. 50명 미만 제안 본문이 교사 API나 UI에 섞여 노출되지 않도록 한다.

시각 시안을 확정하기 전에는 대규모 UI 구현을 시작하지 않는다. 먼저 핵심 화면의 정보 구조와 저해상도 흐름을 사용자에게 검토받는다.

---

## 16. 보안 요구사항

공격자는 외부의 익명 사용자뿐 아니라 **정상적인 학생 계정을 가진 숙련된 사이버 보안 학생**일 수 있다고 가정한다.

### 핵심 보호 자산

- 익명 작성자의 실제 신원
- 학생과 교사의 계정 및 세션
- 누가 어떤 제안에 동의했는지에 대한 비공개 기록
- 심의 결과와 신원 열람 기록
- 정식 안건의 상태와 공식 답변
- 재정·행정 공개 데이터의 무결성
- 암호화 키, API 키, DB 비밀번호, 백업

### 필수 방어

- 기본 거부 방식의 권한 정책
- 요청 수준과 메서드 수준의 이중 권한 검사
- 객체 ID를 바꿔 다른 사용자의 데이터에 접근하는 BOLA/IDOR 테스트
- 일반 학생의 관리자·교사 API 직접 호출 차단
- DTO allowlist와 mass assignment 방지
- CSRF 방어
- 저장형·반사형 XSS 방어
- SQL Injection 방어
- 강한 세션 관리
- 로그인 및 민감 API 속도 제한
- 요청 본문 크기 제한
- 페이지 크기 상한
- 보안 헤더 및 CSP
- 민감정보 로그 마스킹
- 비밀정보의 소스코드·Git 포함 방지
- DB와 백업 보호
- 의존성 취약점 검사
- 운영 오류 응답의 정보 노출 방지
- 외부 URL 연동 시 SSRF 방지와 허용 출처 제한

### 암호화

- 직접 암호 알고리즘을 설계하지 않는다.
- 검증된 플랫폼 또는 라이브러리의 인증된 암호화 방식을 사용한다.
- 암호화 키와 데이터는 분리한다.
- 키 버전을 기록하고 교체 절차를 문서화한다.
- 비밀번호는 암호화가 아니라 Argon2id 해시를 사용한다.
- Base64를 보안 기능으로 취급하지 않는다.

### 감사 로그

최소 다음을 기록한다.

- 로그인 성공·실패와 계정 잠금
- 가입 코드 및 재설정 코드 발급과 사용
- 계정·역할·보직 변경
- 제안 생성과 주요 수정
- 정식 안건 승격
- 교사 공식 상태·답변 변경
- 신고와 심의 생성
- 심의자의 승인·거부
- 신원 열람
- 재정정보 생성·수정·공개
- 보안 설정 변경

감사 로그에 비밀번호, 세션 ID, 가입 코드 원문, 익명 작성자의 평문 신원을 무분별하게 기록하지 않는다. 애플리케이션의 일반 기능으로 감사 로그를 수정하거나 삭제할 수 없게 한다.

### 보안 기준

- OWASP ASVS 5.0 Level 2를 출시 전 검증 기준으로 삼는다.
- OWASP API Security Top 10의 객체·기능 권한 취약점을 집중 점검한다.
- 보안 검증 결과와 미해결 위험을 `SECURITY.md` 또는 별도 문서에 기록한다.

---

## 17. 속도 제한 — Redis 없이 구현

서비스 규모는 전교생과 교사를 합쳐 약 200~220명, 예상 최대 동시 사용자는 약 100~150명이다.

Redis를 도입하지 않는다.

- 단일 서버의 일반 API 속도 제한은 애플리케이션 메모리 기반으로 구현할 수 있다.
- 로그인, 가입 코드, 비밀번호 재설정, 신원 열람 등 보안상 중요한 실패 횟수와 차단 상태는 MySQL에 영속화한다.
- 사용자 ID와 IP를 함께 고려한다.
- 단순 고정 잠금으로 다른 사람의 계정을 쉽게 DoS하지 못하도록 점진적 지연과 운영자 해제 절차를 고려한다.
- 임계값은 코드 여러 곳에 흩어진 상수가 아니라 타입 안전한 설정으로 관리한다.
- 향후 서버가 다중 인스턴스로 확장될 경우에만 Redis 또는 중앙 속도 제한 저장소를 다시 검토한다.

정확한 제안 작성·동의·로그인 제한 수치는 운영 정책으로 조정 가능하게 만들고 README와 설정 예시에 설명한다.

---

## 18. README 필수 목차

루트 `README.md`는 처음 보는 개발자가 별도 구두 설명 없이 로컬 실행과 핵심 구조 이해를 할 수 있을 정도로 작성한다.

최소 다음 항목을 포함한다.

1. 프로젝트 소개와 해결하려는 문제
2. 제품 원칙과 익명성의 의미
3. 주요 사용자와 권한 요약
4. 50명 동의 및 정식 안건 흐름
5. 삭제와 신원 확인 절차
6. 전체 시스템 구조도
7. 디렉터리 구조
8. 기술 스택과 정확한 버전
9. 사전 설치 요구사항
10. 환경변수 전체 목록과 설명
11. MySQL 및 Docker Compose 실행
12. 데이터베이스 마이그레이션 방법
13. 백엔드 로컬 실행
14. 프런트엔드 로컬 실행
15. 최초 슈퍼 어드민 부트스트랩 방법
16. 테스트용 계정과 안전한 시드 데이터 사용법
17. Swagger UI와 OpenAPI JSON 접근 방법
18. OpenAPI 기반 TypeScript 타입 생성 방법
19. 단위·통합·E2E 테스트 실행 방법
20. 주요 설정 변경 방법
21. 보안 설계와 비밀정보 관리
22. 백업과 복구 개요
23. 일반적인 오류와 해결 방법
24. 개발 규칙과 기여 방법
25. 오픈 소스 라이선스와 저작권 고지
26. MVP와 안정 릴리스 이후의 브랜치 전략
27. 보안 취약점 비공개 신고 방법
28. 릴리스와 버전 관리 방법
29. 알려진 제한사항과 향후 과제

README에는 실행 가능한 명령을 복사 가능한 형태로 제공한다. 존재하지 않는 명령이나 아직 구현되지 않은 기능을 완료된 것처럼 문서화하지 않는다.

추가 문서 권장:

- `docs/architecture.md`
- `docs/erd.md`
- `docs/permission-matrix.md`
- `docs/state-machines.md`
- `docs/api-conventions.md`
- `docs/security-model.md`
- `docs/operations.md`
- `docs/branching-and-releases.md`
- `SECURITY.md`
- `CONTRIBUTING.md`
- `CODE_OF_CONDUCT.md`
- `GOVERNANCE.md`
- `LICENSE`
- `NOTICE`

---

## 19. 테스트 요구사항

테스트는 부가 작업이 아니다. 다음 핵심 불변조건에 자동화 테스트를 작성한다.

### 백엔드 단위 테스트

- 가입 코드 일회성 및 만료
- 비밀번호 재설정
- 역할과 보직 유효 기간
- 학생만 동의 가능
- 중복 동의 거부
- 50명 승격 조건
- 허용되지 않은 상태 전이 거부
- 3인 전원 승인 전 신원 열람 거부
- 학생회장·부회장에게 실제 신원 미노출
- 학생부장 외 신원 열람 거부
- 심의 이후 보직 변경이 기존 사건에 영향을 주지 않음
- 공개 제한과 신원 확인의 독립성

### 통합 테스트

- 실제 MySQL 호환 환경을 사용한다. 운영과 다른 동작을 보이는 단순 인메모리 DB에만 의존하지 않는다.
- 동일 학생의 동시 투표 요청에서 한 건만 생성되는지 검증한다.
- 50번째 전후의 병렬 동의에서 정식 안건이 정확히 한 번 생성되는지 검증한다.
- 일반 교사가 50명 미만 제안을 조회할 수 없는지 검증한다.
- 학생이 관리자·교사·심의 API를 직접 호출할 수 없는지 검증한다.
- 객체 ID를 바꿔 다른 학생의 비공개 데이터에 접근할 수 없는지 검증한다.
- 익명 작성자 필드가 일반 응답과 로그에 포함되지 않는지 검증한다.
- 세션, CSRF, 입력 검증, 오류 응답을 검증한다.
- Flyway 마이그레이션을 빈 DB에서 처음부터 적용할 수 있는지 검증한다.

### 프런트엔드 테스트

- 로그인과 최초 활성화
- 익명·실명 제안 작성
- 동의와 철회
- 50명 미만·이상 화면 차이
- 일반 학생, 학생회 임원, 교사, 학생부장, 슈퍼 어드민별 메뉴와 권한
- 키보드 탐색과 주요 접근성 검사
- 오류와 로딩 상태
- 공식 답변과 진행 상태 표시

### E2E 시나리오

1. 신규 학생 계정 활성화
2. 익명 제안 생성
3. 여러 학생의 동의
4. 50명 도달과 정식 안건 승격
5. 교사 공식 답변
6. 실행 상태 완료까지 변경
7. 신고 후 공개 제한 심의
8. 별도 신원 확인 심의
9. 학생회 임원에게 신원이 보이지 않음
10. 학생부장만 재인증 후 신원을 확인
11. 모든 과정이 감사 로그에 남음

### 보안 검증 환경

- 실제 학생 개인정보가 없는 별도 시험 환경과 테스트 계정을 제공한다.
- 화이트 해커 학생들이 운영 서버가 아닌 시험 환경에서 권한 상승, 신원 노출, 중복 투표, 심의 우회를 검증할 수 있게 한다.
- 발견된 취약점은 수정 후 재검증한다.

---

## 20. 구현 순서

다음 순서를 기본으로 하되 기존 저장소 상황에 따라 합리적으로 조정한다.

### 1단계 — 설계 고정

- 요구사항 체크리스트 작성
- 권한 매트릭스 작성
- 상태 전이 문서 작성
- ERD 작성
- 신뢰 경계와 보안 위협 모델 작성
- API 경계 초안 작성
- 사용자 검토가 필요한 모호함 질문

### 2단계 — 프로젝트 기반

- 모노레포 또는 명확한 단일 저장소 구조
- Spring Boot, Next.js, MySQL 개발 환경
- Flyway
- 공통 오류 응답
- 로깅과 traceId
- 기본 OpenAPI/Swagger
- README 초기 버전

### 3단계 — 인증과 권한

- 사용자, 역할, 보직
- 가입 코드와 최초 활성화
- 로그인, 로그아웃, 재설정
- MySQL 세션
- CSRF와 보안 헤더
- 슈퍼 어드민 부트스트랩
- 감사 로그 기반

### 4단계 — 제안과 50명 동의

- 익명·실명 제안
- 학생 전용 목록
- 동의·철회
- DB 고유 제약
- 동시성 안전한 50명 승격
- 처리 상태와 이력

### 5단계 — 교사 처리와 공식 답변

- 정식 안건 교사 조회
- 공식 답변
- 채택·보류·반려·실행·완료 상태
- 학생용 진행 내역

### 6단계 — 신고와 심의

- 신고
- 공개 제한 심의
- 신원 확인 심의
- 임기 기반 고정 심의위원
- 학생부장 단독 신원 열람
- 재인증과 감사

### 7단계 — 비공개 고충 및 재정 공개

- 비공개 개인 고충·안전 제보
- 학교 재정·행정 공개
- 공식 API 어댑터와 수동 입력 경로

### 8단계 — 품질 완성

- 전체 Swagger 문서
- OpenAPI 기반 프런트 타입 생성
- README 및 운영 문서 완성
- 권한·동시성·보안 테스트
- 화이트 해커 검증용 시험 환경 준비
- 접근성 및 UX 점검

### 9단계 — 오픈 소스 공개 준비

- Apache-2.0 `LICENSE`와 정확한 `NOTICE`
- 저작권 주체와 학교명·로고 사용 권한 확인
- `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `GOVERNANCE.md`, `SECURITY.md`
- Issue·Pull Request 템플릿과 `CODEOWNERS`
- 실제 데이터가 없는 예제 설정과 시드
- 전체 Git 기록의 개인정보·비밀정보 검사
- Secret scanning, push protection, Private Vulnerability Reporting 활성화 안내
- MVP Feature Branch 방식과 향후 경량 GitFlow 전환 기준 문서화
- 최초 공개 릴리스의 태그와 변경 내역 준비

각 단계가 끝날 때 빌드, 테스트, 마이그레이션, Swagger와 README를 함께 갱신한다. 문서 작업을 마지막에 몰아서 하지 않는다.

---

## 21. 완료 조건

다음 조건이 모두 충족되어야 완료로 보고한다.

- 저장소를 처음 받은 개발자가 README만으로 로컬 실행할 수 있다.
- 빈 MySQL에서 마이그레이션이 처음부터 성공한다.
- 같은 학생이 같은 제안에 중복 동의할 수 없다.
- 50명 도달 시 정식 안건이 정확히 한 번 생성된다.
- 일반 교사는 50명 미만 제안을 조회할 수 없다.
- 학생회장과 부회장은 학생 기능을 그대로 사용할 수 있다.
- 학생부장·학생회장·부회장 임기가 교체 가능하며 과거 이력이 보존된다.
- 세 명이 모두 승인하기 전에는 신원을 누구도 볼 수 없다.
- 승인 후에도 학생부장만 재인증하여 신원을 볼 수 있다.
- 슈퍼 어드민이 단순 권한만으로 익명 신원을 임의 조회할 수 없다.
- 삭제와 신원 확인이 독립적인 심의로 구현되어 있다.
- 익명 작성자의 내부 정보가 일반 API·로그·Swagger 예시에 노출되지 않는다.
- 모든 API가 Swagger에 실제 구현과 일치하게 문서화되어 있다.
- 프런트엔드 타입이 OpenAPI와 동기화된다.
- README, 보안 문서, ERD, 권한표, 상태 전이 문서가 최신이다.
- 주요 단위·통합·E2E 테스트가 통과한다.
- 프로덕션 비밀정보가 저장소에 없다.
- 실제 학생·교사 데이터, 운영 로그, 익명 신원 연결정보가 공개 저장소와 Git 기록에 없다.
- `LICENSE`, `NOTICE`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `GOVERNANCE.md`, `SECURITY.md`가 서로 모순 없이 준비되어 있다.
- `main` 보호, Pull Request, 필수 CI, CODEOWNERS 검토 규칙이 문서화되어 있다.
- MVP 단계에서 불필요한 `develop`, `release/*`, `hotfix/*`를 만들지 않았다.
- 경량 GitFlow 전환 조건과 전환 이후 병합 규칙이 문서화되어 있다.
- 외부 배포는 수행하지 않았으며 로컬 검증 상태를 명확히 보고한다.

---

## 22. 아직 임의로 확정하면 안 되는 항목

다음은 구현에 필요해지는 시점에 사용자 또는 학교 담당자와 확인한다.

- 서비스 정식 명칭과 로고
- 학교의 정확한 디자인 자산과 공식 색상 사용 여부
- 코드와 문서의 최종 저작권 주체
- 학교명·교표·로고의 오픈 소스 프로젝트 사용 승인 여부
- GitHub Organization의 소유자와 장기 유지관리자
- 교사의 공식 답변 기한
- 제안 동의 모집의 자동 만료 기간
- 정식 안건 담당 교사 지정 방식
- 비공개 개인 고충의 실제 학교 담당자와 열람 범위
- 이의 제기 및 심의 재심 절차
- 법정 또는 학교 규정상 데이터 보존 기간
- 졸업·전학·퇴직 계정의 처리 시점
- 실제 학교알리미 API 키와 제공 가능한 재정 데이터 항목
- 운영 서버, 도메인, TLS, 백업, 비밀관리 환경
- Google/OIDC 로그인 추가 시점
- 첨부파일 기능의 필요 여부

이 항목을 이유 없이 하드코딩하지 않는다. 안전한 비활성 상태, 운영 설정, 명확한 TODO 또는 사용자 질문으로 처리한다.

---

## 23. 최종 보고 형식

작업을 완료하거나 주요 단계가 끝날 때 다음 순서로 보고한다.

1. 사용자 관점에서 완성된 기능
2. 핵심 설계 결정
3. 보안상 중요한 통제
4. 생성·변경된 주요 문서
5. 실행한 빌드와 테스트 결과
6. Swagger와 로컬 실행 방법
7. 아직 남은 결정 또는 알려진 제한

“완료했다”는 표현은 실제 빌드와 테스트가 성공하고 README와 Swagger가 구현과 일치할 때만 사용한다.
